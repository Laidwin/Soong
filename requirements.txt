# Readable mirror of the dependencies. pyproject.toml is the source of truth
# for uv; regenerate this file with:
#   uv export --no-hashes -o requirements.txt

# --- Deep learning core -----------------------------------------------------
# Python 3.13 needs cp313 wheels, so torch 2.6 or later from the CUDA 12.4 index:
#   uv pip install torch torchaudio --index-url https://download.pytorch.org/whl/cu124
torch>=2.6
torchaudio>=2.6

# --- Video download ---------------------------------------------------------
yt-dlp>=2024.1.0

# --- Lyrics lookup ----------------------------------------------------------
lyricsgenius>=3.0.1

# --- Forced alignment, the only source of timing (MMS model) ----------------
ctc-forced-aligner @ git+https://github.com/MahmoudAshraf97/ctc-forced-aligner.git

# --- Desktop UI (Qt, LGPL, so compatible with shipping an executable) -------
PySide6>=6.6.0

# --- Configuration and utilities --------------------------------------------
pyyaml>=6.0
python-dotenv>=1.0.0
rich>=13.0
typer>=0.12.0

# --- Standalone executable packaging ----------------------------------------
pyinstaller>=6.3.0

# ---------------------------------------------------------------------------
# The transcription fallback reuses the code of the Lyricsmith project from a
# local checkout (imported through sys.path, never pip installed). Its heavy
# dependencies are listed here so the fallback works.
# ---------------------------------------------------------------------------
demucs>=4.0.0
transformers>=4.40.0,<5.0
soundfile>=0.12.1
librosa>=0.10.0
scipy>=1.13.0
pyloudnorm>=0.1.1
noisereduce>=3.0.0
huggingface_hub>=0.24.0
