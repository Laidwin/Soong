# Copy this file to `.env` and fill in the values.

# Genius API access token (https://genius.com/api-clients).
# Without it, the lyrics always come from the local transcription fallback.
GENIUS_ACCESS_TOKEN=

# (Optional) Path to an alternative config.yaml.
# SOONG_CONFIG=./config.yaml
